const { expect } = require("chai"); 
const { ethers } = require('hardhat');

//SEED = 0 and 10**10;
//SPROUT = 1 and 10**10;
//SAPLING = 2 and 10**10;
//TREE = 3 and 10**10;
//HIO = 4 and 10**10;


describe('Marketplace', function () {
    it("Testing the marketplace interaction with tokens", async function () {
      
      //const accounts = await ethers.getSigners();
      
      //set up our rewild contract
      //const Rewild = await ethers.getContractFactory('Rewild');
      //const reWild = await Rewild.deploy();
      //await reWild.deployed();
      //reWildAddress = reWild.address;

      //and now set up the marketplace contract
      //const Marketplace = await ethers.getContractFactory('Marketplace');
      //const marketplace = await Marketplace.deploy();
      //await marketplace.deployed();
      //marketplaceAddress = marketplace.address;

      //do

      

    });
  });